/**
 * Displays a message to STDOUT
 * @param {str} msg to be displayed
 * @authour Nicholas Otieno <https://github.com/Nicholas2023>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
